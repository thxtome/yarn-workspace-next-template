import type { NextPage } from "next";
import Typography from "@shared/components/Typography";
import HStack from "@shared/components/HStack";

const Home: NextPage = () => {
    return (
        <HStack>
            <Typography kind="display2">Home</Typography>
        </HStack>
    );
};

export default Home;
